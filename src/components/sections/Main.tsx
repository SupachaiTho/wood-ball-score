import Section from '@/components/base/Section';
import GameInfo from '@/components/main/GameInfo';
import MainBoard from '@/components/main/MainBoard';

const Main = () => {
  return (
    <Section>
      <GameInfo />
      <MainBoard />
    </Section>
  );
};

export default Main;
