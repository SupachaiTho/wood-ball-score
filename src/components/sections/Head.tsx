import Section from '@/components/base/Section';
import HeadTitle from '@/components/head/HeadTitle';

const Head = () => {
  return (
    <Section>
      <HeadTitle />
    </Section>
  );
};

export default Head;
